import { FilterNsortPipe } from './filterNsort.pipe';

describe('SortPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNsortPipe();
    expect(pipe).toBeTruthy();
  });
});
